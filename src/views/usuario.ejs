<%- include('partials/_header') %>

   <div class="container">
       <div class="row">
           <!--Form-->
           <div class="col-md-5">
               <div class="card">
                   <div class="card-body">
                       
                    <form action="/compra" method="POST">  <!--mediante metodo post llamo mi archivo add-->
                            
                            <div class="form-group">
                            <input id="este" type="text" name="buscar" placeholder="Busqueda" class="form-control mb-2 d-grid gap-2">
                            </div>

                           <div class="d-grid gap-2">
                            <button  tupe="submit" class="btn btn-primary " onclick="obtener()">Buscar</button>
                           </div>
                           <p id="valueInput"></p> 
<script>
const obtener=()=>{
    let $el=document.querySelector("#este").value; 
   
    document.querySelector("#valueInput").innerHTML =$el;
}    
</script>

                    </form>
                   </div>
               </div>
           </div>
           <!--list-->
            

           <div class="col-md-7">
               <table class="table table-bordered table-hover">
                   <thead>
                       <tr>
                           <th>N°</th>
                           <th>Nombre</th>
                           <th>Precio</th>
                           <th>Tags</th>
                           <th>Cantidad</th>
                           
                       </tr>
                   </thead>

                   <tbody><!--recorro tasks e imprimo-->
                        <% for(var i=0; i<televisores.length; i++) {%> 
                        <tr>
                            <td><%= i+1 %> </td>
                            <td><%= televisores[i].nombre %> </td>
                            <td><%= televisores[i].precio %> </td>
                            <td><%= televisores[i].tags %> </td>
                            <td><%= televisores[i].cantidad %> </td>
                           
                        </tr>
                       <% } %>  

                   </tbody>
               </table>
           </div>

           <div>
            <a href="/" class="btn btn-info">Home</a>
            </div>

       </div>
   </div>

<%- include('partials/_footer') %>

